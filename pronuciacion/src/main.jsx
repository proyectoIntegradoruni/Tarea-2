import React from 'react';
import ReactDOM from 'react-dom'; // Corregir la importación de ReactDOM
import AppI from './appf';
ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <AppI />
  </React.StrictMode>
);
